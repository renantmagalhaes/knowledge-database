==Stop / Remove all docker containers/images at once==

#Stop
docker stop $(docker ps -a -q)

#Remove
docker rm $(docker ps -a -q)

#Remove images
docker rmi $(docker images -q)

#Deleting all files
service docker stop
rm -rf /var/lib/docker
service docker start
